#include "main.h"
/**
 * main - prints the alphabet in lower case
 *
 * Return: Always 0
 */

void print_alphabet(void);
{
	char a = 'a'
		for (a = 'a'; a <= 'z'; a++)
		{
			_putchar(a)
}

return (0);
}
