#include "main.h"

/**
 * add - is a function that prints every min of the day
 * @x: the first num to add
 * @z: the sec num to add
 * Return: A;ways value
 **/
int add(int z, int x)
{
	int value = z + x;

	return (value);
}
