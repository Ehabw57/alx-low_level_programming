#include <stdio.h>
/**
* main - this function prints it is own name
* @argc: is the argc
* @argv: is the argv
* Return: alway 0
**/
int main(int c, chacoins *v[])
{
(void)c;
printf("%s\n", v[0]);
return (0);
}
