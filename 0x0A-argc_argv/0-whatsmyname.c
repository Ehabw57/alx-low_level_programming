#include <stdio.h>
/**
 * main - this function prints it is own name
 * @c: is the argc
 * @v: is the argv
 * Return: alway 0
 **/
int main(int c, char *v[])
{
(void)c;
printf("%s\n", v[0]);
return (0);
}
