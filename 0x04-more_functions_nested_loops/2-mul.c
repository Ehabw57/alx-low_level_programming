#include "main.h"

/**
* mul - checkes if c is a digit or somthing else
* @a: the variable 1
* @b: the variable 2
* @r- the fainal result
* Return: (mul)
*/
int mul(int a, int b)
{
	int r = a * b;

	return (r);
}
