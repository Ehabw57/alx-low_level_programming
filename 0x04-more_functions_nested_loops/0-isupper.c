#include "main.h"

/**
* _isupper - cheecks for c if it id upper or lower case
* @c: the variable to chaeck
* Return: (1) if c id upper (0) id else
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
}
