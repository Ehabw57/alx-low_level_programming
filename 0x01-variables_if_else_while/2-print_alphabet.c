#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - is a function that prints a alphaber characters
 * Return: 0 (success)
 */
int main(void)
{
	int n = 97;
	int z = 123;

	while (n != z)
{
	putchar(n);
	n++;
}

return (0);
}
