#include <stdio.h>
#include <time.h>
/**
 * main - is a functiun that prints numer statment
 * RETURN (0) (sucsess)
 */
void main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX /
		if (n < 0)
		{
			printf("%i is negative", n);
				if else(n = 0)
				printf("%i is zero", n);
				else
				printf("%i is positive", n);
		}
	return (0);
}
