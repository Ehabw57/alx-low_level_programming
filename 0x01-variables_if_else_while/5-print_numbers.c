#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - is a function that prints a alphaber characters
 * Return: 0 (success)
 */
int main(void)
{
	int n = 0;
	int z = 10;

	while (n != z)
{
	printf("%d", n);
	n++;
}
putchar('\n');

return (0);
}
